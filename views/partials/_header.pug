
header.header
  nav.nav
    .container.container--navbar
      a#nav__brand.nav__brand(href="/") Connect 
        span.text-secondary Four
      ul.nav__el
        if user 
          li.nav__el__cta.nav__el__cta--primary
            a(href="/my-saved-games") Saved games
          li.nav__el__cta
            a#logout logout
          li.nav__el__cta
            a(href="/updateUser")

              //- img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
              span= user.displayName
        else
          li.nav__el__cta.nav__el__cta
            a(href="/login") login
          li.nav__el__cta
            a(href="/register") sign up


